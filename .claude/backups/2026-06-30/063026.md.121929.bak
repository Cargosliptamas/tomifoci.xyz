# 063026 - Daily Work Log

## Decisions
- Classic-game fallback at `/classic`: PLAN ONLY for now (build deferred). Plan in `CLASSIC_FALLBACK_PLAN.md`.
- "Purge Convex" = docs wording only (banners on CLAUDE.md/AGENTS.md/PROJECT-GOVERNOR.md in classic repo); historical journals left intact.
- Cleanup sequencing: S1 security fix FIRST, then full-unify cleanup (engine = source of truth).

## Meetings & Conversations
-

## Notes
- Classic repo `Vb_Tippjatek_2026` is 3-in-1: original Convex+Cloudflare game (root `js/`+`convex/`), `apps/rewrite/` (Neon, same Vercel project as tomifoci.xyz), shared `packages/game-engine`. Convex retired; Neon (blue-poetry-39540165) is the only live DB.
- Code review: 4 of 5 suspected bugs already fixed. Real finds: S1 (PIN brute-force, FIXED), C1 dup scoring engine, C2 triple match-data defs, kickoffOdds dead tier.
- S1 fixed + tested (9/9 pass, tsc clean).

## End of Day Summary
- Docs: purged stale "Convex is current" framing from classic repo (CLAUDE.md/AGENTS.md/PROJECT-GOVERNOR.md). Built KB of classic rules + classic-fallback build plan.
- Security: fixed S1 PIN brute-force hole (shared throttle across favorites/predictions/wizard/auth) + tests.
- Test matches: removed test leaderboard (UI+compute), confirmed frontend already clean (fixtures has none), fixed MATCH_META egg opponent bug (1005/1006), parity guard added. DB deletion deemed unnecessary by user.
- Cleanup: C1 full engine-unify (deleted duplicate scoring impl + data.js runtime parse), C4 dead exports, C6 clampOdds dedupe. All via tests.
- S2 fixed: removed IP from throttle key (x-forwarded-for is client-spoofable; per-account lockout is now IP-agnostic). Affected: throttle.ts, player-pin/route.ts, change-pin/route.ts.
- kickoffOdds fixed: runLiveScorePoll now reads previous 'odds' cache before each poll and freezes odds for matches that disappeared from the fixtures feed into a cumulative 'kickoffOdds' apiCache row. repairOdds() tier 1 (snapshot) is now live.
- C3 push: intentionally parked ("dormant over gated"). Module self-documents activation path. No wiring needed.
- Verified: next build ✅, tsc ✅, 14/14 tests.
- Open: S3 claim-on-first-login pre-emptive takeover. C2 livescore.ts still parses data.js (parity guard covers drift).
