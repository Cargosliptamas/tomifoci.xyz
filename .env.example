# Neon (Postgres) — project blue-poetry-39540165
# Set the real value in Vercel → Settings → Environment Variables (Production).
DATABASE_URL="postgresql://USER:PASSWORD@HOST/neondb?sslmode=require&channel_binding=require"
